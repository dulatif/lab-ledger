# 2. Core SQL Syntax and Data Types

## 🎯 Learning Goal

Speaking the language.

## 🧠 Concept

**Keywords**: `SELECT`, `FROM`, `WHERE` (Case insensitive, but conventionally UPPERCASE).
**Terminator**: Semicolon `;`.

**Data Types**:

- `INT`: Integers (1, 2, 3).
- `VARCHAR(n)`: Strings (Variable length).
- `BOOLEAN`: True/False.
- `DATE`: YYYY-MM-DD.

## 💻 Implementation

```sql
SELECT name FROM users;
```

## 🧩 Activity / Challenge

1.  Which type would you use for:
    - Age? (`INT`)
    - Email? (`VARCHAR`)
    - Is Active? (`BOOLEAN`)

## 🔑 Key Takeaways

- Choosing the right data type saves space and prevents errors.
